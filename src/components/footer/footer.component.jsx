import React from 'react';
import './footer.styles.scss';

const Footer = props => {
    return(
        <footer>
            <p>this is the footer</p>
        </footer>
    );
}

export default Footer;